<div class="row">
  <div class="col-md-8 offset-md-2 card-body card-padding text-right"><%= link_to "Importar datos", new_import_sale_path , class: "btn btn-success pull-right"%></div>
  <div class="col-md-8 col-sm-12 offset-md-2 text-center">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Datos importados</th>
          <th>Fecha importación</th>
          <th>Operaciones</th>
        </tr>
      </thead>
      <tbody>
        <% @import_sales.each do |is|  %>
          <tr>
            <td><%= is.import_quantity %></td>
            <td><%= is.created_at.strftime("%d/%m/%Y") %></td>
            <td>
              <%= link_to "Ver", import_sale_path(is) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
